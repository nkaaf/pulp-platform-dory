Following are the changes, that are made to the original implementation:

- setup.py:
    - Update version
    - update package_data
- .gitmodules:
    - change repo url to build wheel without public key authorization
